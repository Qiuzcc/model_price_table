# 复制本文件为 .env 并填入真实值：cp .env.example .env

# Artificial Analysis 免费 API Key（可选）
# 获取方式：https://artificialanalysis.ai/data-api → Create a free API key（注册登录后在 API key management 生成）
# 作用：启用对比表格的「输出速度」「首 Token 延迟」两列（服务端专用，切勿提交到仓库）
# 注意：填写后需重启 dev server 才会生效；留空时两列显示 —，其余功能不受影响
ARTIFICIAL_ANALYSIS_API_KEY=
